{
  "name": "bugsafari-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "testing-core",
    "developer-dashboard",
    "shared"
  ],
  "scripts": {
    "build": "npm run build -w testing-core && npm run build -w developer-dashboard",
    "typecheck": "npm run typecheck -w shared && npm run typecheck -w testing-core && npm run build -w developer-dashboard",
    "test": "npm run test -w shared && npm run test -w testing-core && npm run test -w developer-dashboard",
    "dev:server": "npm run dev -w testing-core",
    "dev:client": "npm run dev -w developer-dashboard"
  },
  "devDependencies": {
    "tsc-watch": "^7.2.0",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "lucide-react": "^1.25.0"
  }
}
