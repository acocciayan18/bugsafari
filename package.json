{
  "name": "bugsafari-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "shared",
    "testing-core",
    "developer-dashboard"
  ],
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "build": "npm run build -w shared --if-present && npm run build -w testing-core && npm run build -w developer-dashboard",
    "build:dashboard": "npm run build -w developer-dashboard",
    "typecheck": "npm run typecheck -w shared --if-present && npm run typecheck -w testing-core && npm run build -w developer-dashboard",
    "test": "npm run test -w shared --if-present && npm run test -w testing-core && npm run test -w developer-dashboard",
    "dev:server": "npm run dev -w testing-core",
    "dev:worker": "npm run dev:worker -w testing-core",
    "dev:client": "npm run dev -w developer-dashboard",
    "lint": "eslint .",
    "format": "prettier --write \"testing-core/src/**/*.ts\" \"shared/**/*.ts\""
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "eslint": "^9.39.5",
    "prettier": "^3.9.6",
    "tsc-watch": "^7.2.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.66.0"
  },
  "dependencies": {
    "driver.js": "^1.8.0",
    "framer-motion": "^12.43.0",
    "lucide-react": "^1.25.0"
  }
}
