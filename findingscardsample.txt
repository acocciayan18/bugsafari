SPA State Race Condition
Medium

CWE-362
Status
NEEDS VERIFICATION 70%
Message
POST /api/counter/cas was sent again 1ms later while the first was still running.
API Endpoint
POST /api/counter/cas
Reproduction Playbook

1
open
Navigate to /state-races
2
rapid
Click 3 controls at the same time: the "🦁 BugSafari Target" link, the "← all scenarios" link and the "Increment (unguarded)" button
Route
/state-races
Suggested Fix

The control fired the same state-changing request twice with no debounce or disable-on-submit guard.
Suggested fix: guard against overlapping actions
1. Disable or debounce the control after the first interaction until it finishes
2. Make each state change safe to run more than once under overlapping events
3. Add a test that fires a fast burst and confirms only one result is committed



SPA State Race Condition
Medium

CWE-362
Status
NEEDS VERIFICATION 70%
Message
POST /api/counter was sent again 1ms later while the first was still running.
API Endpoint
POST /api/counter
Reproduction Playbook

1
open
Navigate to /state-races
2
rapid
Click 3 controls at the same time: the "Increment (compare-and-set 409)" button, the "🦁 BugSafari Target" link and the "← all scenarios" link
Route
/state-races
Suggested Fix

The control fired the same state-changing request twice with no debounce or disable-on-submit guard.
Suggested fix: guard against overlapping actions
1. Disable or debounce the control after the first interaction until it finishes
2. Make each state change safe to run more than once under overlapping events
3. Add a test that fires a fast burst and confirms only one result is committed



SPA State Race Condition
Medium

CWE-362
Status
NEEDS VERIFICATION 70%
Message
POST /api/counter was sent again 0ms later while the first was still running.
API Endpoint
POST /api/counter
Reproduction Playbook

1
open
Navigate to /state-races
2
rapid
Click 3 controls at the same time: the "Increment (compare-and-set 409)" button, the "🦁 BugSafari Target" link and the "← all scenarios" link
Route
/state-races
Suggested Fix

The control fired the same state-changing request twice with no debounce or disable-on-submit guard.
Suggested fix: guard against overlapping actions
1. Disable or debounce the control after the first interaction until it finishes
2. Make each state change safe to run more than once under overlapping events
3. Add a test that fires a fast burst and confirms only one result is committed


