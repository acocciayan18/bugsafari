Runtime Stability Exception
Medium
×6
Verify Fix

CWE
CWE-248
Status
CONFIRMED 89%
Message
TypeError: Cannot read properties of null (reading 'name')
Element
Add
Reproduction Playbook

1
open
Navigate to /
2
rapid
Click 5 controls at the same time: the "Add" button, the "Add" button, the "Add" button, the "Sign in" link and the "☕" link
Route
/
Suggested Fix
See More Suggestions

The code read a field on `null`, usually a missing page element (`querySelector` returned null) or an empty API result. Confirm the lookup found something before using it.

Suggested fix:
1. Reproduce the fault using the replay checklist above
2. Apply that guard at the failing line
3. Add a test that confirms it stays stable

Stack Trace
Click to expand
Runtime Stability Exception
Medium
Verify Fix

CWE
CWE-248
Status
CONFIRMED 89%
Message
TypeError: Cannot read properties of null (reading 'name')
Reproduction Playbook

1
open
Navigate to /
2
rapid
Click 5 controls at the same time: the "Add" button, the "Add" button, the "Add" button, the "Sign in" link and the "☕" link
Route
/
Suggested Fix
See More Suggestions

The code read a field on `null`, usually a missing page element (`querySelector` returned null) or an empty API result. Confirm the lookup found something before using it.

Suggested fix:
1. Reproduce the fault using the replay checklist above
2. Apply that guard at the failing line
3. Add a test that confirms it stays stable

Stack Trace



*******************


Runtime Stability Exception
Medium
×6

CWE
CWE-248
Status
CONFIRMED 89%
Message
TypeError: Cannot read properties of null (reading 'name')
Reproduction Playbook

1
open
Navigate to /
2
rapid
Click 5 controls at the same time: the "Add" button, the "Add" button, the "Add" button, the "Sign in" link and the "☕" link
Route
/
Suggested Fix

The code read a field on `null`, usually a missing page element (`querySelector` returned null) or an empty API result. Confirm the lookup found something before using it.

Suggested fix:
1. Reproduce the fault using the replay checklist above
2. Apply that guard at the failing line
3. Add a test that confirms it stays stable

Stack Trace

