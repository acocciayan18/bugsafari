// Opt-in shared slot for run/connection status toasts — callers passing this id
// replace the prior status message instead of stacking. Other toasts stay distinct.
export const TOAST_ID = 'bugsafari-toast';
