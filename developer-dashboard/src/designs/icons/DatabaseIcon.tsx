import { Database } from 'lucide-react';

export const DatabaseIcon = ({ className = 'w-4 h-4' }: { className?: string }) => (
    <Database className={className} strokeWidth={1.75} aria-hidden="true" />
);
