import { EyeOff } from 'lucide-react';

export const EyeSlashIcon = ({ className = 'w-4 h-4' }: { className?: string }) => (
    <EyeOff className={className} strokeWidth={1.75} aria-hidden="true" />
);
