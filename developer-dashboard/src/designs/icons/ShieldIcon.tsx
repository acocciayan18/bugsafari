import { Shield } from 'lucide-react';

export const ShieldIcon = ({ className = 'w-4 h-4' }: { className?: string }) => (
    <Shield className={className} strokeWidth={1.75} aria-hidden="true" />
);
